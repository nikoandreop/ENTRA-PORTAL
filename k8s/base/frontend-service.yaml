apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: entra-portal-system
spec:
  selector:
    app: entra-portal-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
